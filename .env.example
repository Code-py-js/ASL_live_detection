# Example environment variables for backend configuration

# MongoDB connection string (e.g., mongodb+srv://<user>:<pass>@cluster0.mongodb.net/dbname)
MONGODB_URI=

# JWT secret for signing auth tokens
JWT_SECRET=

# Optional: Port for backend server
PORT=4000

# Optional: URL for OTA model updates
MODEL_OTA_URL=https://example.com/models/
